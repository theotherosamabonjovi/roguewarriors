// ============================================================
//  ROGUE WARRIORS DIGITAL — MAIN ENTRY
// ============================================================

document.addEventListener('DOMContentLoaded', () => {
  // Set default army builder button states
  UI.setTheme('urban');
  UI.setDensity('medium');
  console.log('Rogue Warriors Digital — ready.');
});
